# Starting baseline: Tilt Web UI Dependency Graph

The mission is to learn enough Tilt Web UI internals to build a Dependency Graph View independently.
The user already has local notes on the rendering stack, dependency data source, graph node Storybook workflow, and the implementation exercise, so future lessons should build on those instead of reteaching the project from zero.
